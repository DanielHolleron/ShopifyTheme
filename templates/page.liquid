{% comment %}
   The template for regular web pages.
{% endcomment %}

<div id="shopify_page" class="section section-page">
  
   <div class="container">

      <div class="has-text-centered">
         <h1 class="title is-3">{{ page.title }}</h1>
         <div>{{ page.content }}</div>
      </div>

   </div>
  
</div>